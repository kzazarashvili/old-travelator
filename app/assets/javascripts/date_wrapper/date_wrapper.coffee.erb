$ ->
  assign_datepickers()

@assign_datepickers = (target = 'body') ->
  $(target).find('.datepicker').datepicker({
    format: 'MM d, yyyy',
    autoclose: true,
    todayHighlight: true,
    weekStart: 1
  })
