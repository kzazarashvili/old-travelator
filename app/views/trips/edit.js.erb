$('#trip_<%= @trip.id %>').replaceWith("<%= j render 'edit', trip: @trip %>");

$(document).ready(function(){
  window.setupCountriesSelect()
  window.setupDatePicker('#edit_trip_<%= @trip.id %>');
});
