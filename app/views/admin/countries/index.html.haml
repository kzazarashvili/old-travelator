#heading-container
  .d-flex.flex-row.justify-content-between
    %h1= t('.title')
    = link_to t('.create'), new_admin_country_path, class: 'btn btn-primary align-self-center'

  = render 'admin/components/search', model_name: :countries

%table.table.table-hover
  %thead
    %tr
      %th= order_by_link 'Id'
      %th= order_by_link 'Name'
      %th= order_by_link 'Abbreviation'
      %th

  %tbody
    - @countries.each do |country|
      %tr
        %td= link_to country.id, [:admin, country]
        %td= country.name
        %td= country.abbreviation
        %td.text-right
          = render 'admin/components/view_edit_delete', object: country

= paginate @countries
