%h1 This is index countries page for admin

= link_to 'New country', new_admin_country_path, class: 'btn btn-primary'

.col-md-12
  = render 'shared/admin/search_form', resource: :countries

- @countries.each do |country|
  .p-2= country.name
  .ml-auto.p-2
    = link_to 'Edit', [:edit, :admin, country], class: 'btn btn-primary'
    = link_to 'Delete', [:admin, country], method: :delete,
                                data: { confirm: 'Are you sure, that you wanna delete this country' }, class: 'btn btn-danger'
    = link_to 'Show page', [:admin, country], class: 'btn btn-info'
